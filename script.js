const  serachInput = document.getElementById('search-input');

document.addEventListener('input', function() {
     const  searchTerm = serachInput.value.toLoweCase();
     if (searchTerm === '') {
        
     }
})